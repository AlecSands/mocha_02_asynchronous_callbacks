var chai = require('chai');
var should = chai.should();
var callbackFunction = require('../server/module/callback.js');

describe('Testing a Callback Function', function() {
  context('callback.js', function() {

  });
});
